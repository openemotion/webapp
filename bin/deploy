#!/bin/sh
ssh openemotion@alpha.openemotion.org /bin/bash <<EOF
cd ~/webapp
git pull
echo -n "last commit: "
git --no-pager log -1 --pretty="format:%s (%an)%n"
sudo supervisorctl restart openemotion
sudo nginx -s reload
echo nginx: reloaded
EOF
