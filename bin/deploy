#!/bin/sh
if [ `whoami` <> "staging" ]; then
    echo "please run this script from staging@alpha.openemotion.org"
    exit 1
fi
sudo -u openemotion "cd /home/openemotion/webapp && git pull"
sudo supervisorctl restart openemotion
sudo nginx -s reload
echo nginx: reloaded
